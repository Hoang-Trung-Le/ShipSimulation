%% Waypoints of paths
function waypoints = PathWaypoints()

waypoints = [500 0;
            500 1000;
            1000 1500;
            2000 0];

% s = size(waypoints,1);
% for i = 1:(s-1)
%    xPaths = 
% end

end

% plot(waypoints(1:4,2), waypoints(1:4,1))